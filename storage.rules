rules_version = '2';

function isAdmin() {
  return exists(/databases/(default)/documents/users/admins/$(request.auth.uid));
}

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if isAdmin();
    }
  }
}
