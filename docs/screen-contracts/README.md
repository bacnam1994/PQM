# SCREEN & UI BEHAVIOR CONTRACTS (LEVEL 4)

Thư mục này đặc tả chi tiết 25 màn hình và hợp đồng hành vi tương tác UI của PQM:

- `SCREEN_INVENTORY.md`: Danh mục tổng hợp 25 màn hình định danh.
- Thư mục `screens/`: Hợp đồng chi tiết cho từng màn hình (`SC-01` đến `SC-25`).
