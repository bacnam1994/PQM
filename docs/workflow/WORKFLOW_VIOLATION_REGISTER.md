# PQM — WORKFLOW VIOLATION REGISTER

> **Sổ tay ghi nhận toàn bộ vi phạm và biện pháp khắc phục kiến trúc Workflow (WF-001 đến WF-022)**

---

## BẢNG TỔNG HỢP VÀ TÌNH TRẠNG XỬ LÝ

| Violation ID | Vị trí (File & Line)                                                                         | Hành vi ban đầu (Current)                                           | Hành vi chuẩn (Expected)                                                                 | Quy tắc vi phạm    | Nguyên nhân gốc rễ (Root Cause)                                 | Giải pháp khắc phục (Fix)                                                                                                                         | Regression Test                                     |   Tình trạng    |
| :----------- | :------------------------------------------------------------------------------------------- | :------------------------------------------------------------------ | :--------------------------------------------------------------------------------------- | :----------------- | :-------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------ | :-------------------------------------------------- | :-------------: |
| **WF-001**   | `useTestResultSave.ts`                                                                       | Lưu phiếu PASS tự đổi Batch sang `RELEASED`                         | Lưu phiếu chỉ cập nhật QualityStatus; Batch giữ nguyên `TESTING`                         | Section 5, Model 1 | Tự động hóa quá mức tại hook lưu phiếu                          | Xóa lệnh `updateBatchStatus(batchId, RELEASED)` khỏi save flow                                                                                    | `tests/domain/workflowRegression.test.ts`           | ✅ **RESOLVED** |
| **WF-002**   | `useTestResultSave.ts`                                                                       | Lưu phiếu FAIL tự đổi Batch sang `REJECTED`                         | Lưu phiếu chỉ cập nhật QualityStatus; Batch giữ nguyên `TESTING`                         | Section 5, Model 1 | Tự động hóa quá mức tại hook lưu phiếu                          | Xóa lệnh `updateBatchStatus(batchId, REJECTED)` khỏi save flow                                                                                    | `tests/domain/workflowRegression.test.ts`           | ✅ **RESOLVED** |
| **WF-003**   | `useTestResultForm.ts`                                                                       | Chọn hoặc mở Lô tự đổi Batch sang `TESTING`                         | Chọn hoặc mở Lô là thao tác Read-only, không làm thay đổi trạng thái                     | Section 7, FSM     | Side-effect trong `handleBatchSelect`                           | Xóa lệnh `updateBatchStatus` khi chọn lô                                                                                                          | `src/architecture/workflowMutationGuard.test.ts`    | ✅ **RESOLVED** |
| **WF-004**   | `batchActionTools.ts:96`<br>`useTestResultAIIntegration.ts:372`<br>`AIAssistantChat.tsx:794` | Tạo lô mới tự gán `status: TESTING`                                 | Lô sản xuất mới tạo bắt buộc có trạng thái `PENDING`                                     | Section 9 & 10     | Gán tắt trạng thái khi tạo tự động                              | Chuẩn hóa tất cả các luồng tạo mới thành `PENDING`; chuyển sang `TESTING` qua action `START_TESTING`                                              | `tests/domain/batchWorkflowRegression.test.ts`      | ✅ **RESOLVED** |
| **WF-005**   | `BatchAppService.ts:105`                                                                     | `updateBatch()` có thể sửa trường `status`                          | `updateBatch()` chỉ sửa dữ liệu hành chính; cấm sửa status                               | Section 11 & 12    | Generic CRUD thiếu guard kiểm tra thay đổi status               | Bổ sung guard: chặn nếu `batch.status !== old.status`                                                                                             | `src/architecture/workflowMutationGuard.test.ts`    | ✅ **RESOLVED** |
| **WF-006**   | `database.rules.json`                                                                        | Firebase Rules cho phép user bình thường sửa status                 | Chỉ QA/ADMIN được chuyển status sang `RELEASED`, `REJECTED`, `BLOCKED`, `PENDING` (CAPA) | Section 13 & 14    | Rules chưa phân biệt rõ ràng Data Mutation và Workflow Mutation | Siết chặt database rules cho `batches`: cấm bypass FSM                                                                                            | `tests/security/workflowBypass.test.ts`             | ✅ **RESOLVED** |
| **WF-007**   | `BatchRules.ts` & `ReleaseRules.ts`                                                          | Hai nơi cùng có logic đánh giá Release độc lập                      | `ReleaseRules.evaluateReleasePrerequisites()` là SSoT duy nhất                           | Section 15 & 16    | Phân tán logic kiểm tra 7 Release Gates                         | Chuyển `BatchRules.canRelease` sang ủy quyền trực tiếp cho `ReleaseRules`                                                                         | `tests/domain/workflowRegression.test.ts`           | ✅ **RESOLVED** |
| **WF-008**   | `BatchAppService.ts:212`                                                                     | Reject chưa bắt buộc lý do giải trình rõ ràng                       | Reject bắt buộc QA/ADMIN + Lý do giải trình bắt buộc                                     | Section 17 & 18    | Thiếu validation lý do ở mức Service                            | Bổ sung kiểm tra `!options?.reason` khi reject lô                                                                                                 | `tests/domain/batchWorkflowRegression.test.ts`      | ✅ **RESOLVED** |
| **WF-009**   | `stateMachine.ts:114`                                                                        | `BatchStateMachine` thiếu role requirement một số transition        | Mọi regulated transition bắt buộc QA hoặc ADMIN                                          | Section 19         | `QA_ADMIN_REQUIRED_TRANSITIONS` chưa bao quát đầy đủ            | Bổ sung đầy đủ ma trận quyền hạn cho FSM                                                                                                          | `tests/domain/batchWorkflowRegression.test.ts`      | ✅ **RESOLVED** |
| **WF-010**   | `stateMachine.ts:154`                                                                        | `REJECTED -> PENDING` chưa khóa đúng actor & CAPA                   | Chỉ QA/ADMIN có lý do xét duyệt CAPA mới được mở lại                                     | Section 20         | Thiếu ràng buộc lý do giải trình CAPA                           | Bắt buộc QA/ADMIN + Lý do CAPA                                                                                                                    | `tests/domain/batchWorkflowRegression.test.ts`      | ✅ **RESOLVED** |
| **WF-011**   | `stateMachine.ts:165`                                                                        | `BLOCKED -> TESTING` chưa khóa đúng actor & retest plan             | Chỉ QA/ADMIN có kế hoạch retest mới được unblock                                         | Section 21         | Thiếu ràng buộc lý do mở khóa                                   | Bắt buộc QA/ADMIN + Kế hoạch retest                                                                                                               | `tests/domain/batchWorkflowRegression.test.ts`      | ✅ **RESOLVED** |
| **WF-012**   | `TestResultAppService.ts:262`                                                                | Phê duyệt TestResult (`FINAL -> APPROVED`) thiếu bắt buộc chữ ký số | Bắt buộc QA/ADMIN + Chữ ký số 21 CFR Part 11 hợp lệ                                      | Section 22 & 23    | Thiếu bước xác thực chữ ký điện tử trong Service                | Bắt buộc truyền `options.signature` và verify tính toàn vẹn SHA-256                                                                               | `tests/security/workflowBypass.test.ts`             | ✅ **RESOLVED** |
| **WF-013**   | `stateMachine.ts:460`                                                                        | `SUPERSEDED` thiếu role guard                                       | Chỉ QA/ADMIN có lý do kiểm nghiệm lại mới được đánh dấu `SUPERSEDED`                     | Section 24         | QC/LAB có thể tự đánh dấu superseded                            | Bổ sung `ROLE_REQUIREMENTS` và lý do bắt buộc cho `SUPERSEDED`                                                                                    | `tests/domain/testResultWorkflowRegression.test.ts` | ✅ **RESOLVED** |
| **WF-014**   | Architecture Layering                                                                        | Generic repository status mutation chưa có static guard             | UI không được gọi trực tiếp `repository.updateStatus`                                    | Section 26 & 35    | Thiếu kiểm tra tĩnh kiến trúc                                   | Tạo static architecture guard kiểm tra production source                                                                                          | `src/architecture/workflowMutationGuard.test.ts`    | ✅ **RESOLVED** |
| **WF-015**   | UI entry points                                                                              | UI truyền status tùy tiện khắp nơi                                  | Mọi UI entry point phải thông qua Workflow Action contract                               | Section 27 & 28    | Thiếu contract chuẩn hóa cho các hành động Workflow             | Định nghĩa `src/domain/workflow/workflowActions.ts`                                                                                               | `tests/domain/workflowRegression.test.ts`           | ✅ **RESOLVED** |
| **WF-016**   | `batchActionTools.ts:204`                                                                    | AI có khả năng trực tiếp gọi `store.updateBatchStatus`              | AI chỉ được ĐỀ XUẤT (Proposal Only); người có thẩm quyền ký duyệt                        | Section 29 & 30    | AI Action tool thiếu chốt chặn proposal bắt buộc                | Đổi công cụ AI sang trả về Proposal, xóa bỏ trực tiếp store mutation                                                                              | `src/architecture/workflowMutationGuard.test.ts`    | ✅ **RESOLVED** |
| **WF-017**   | `ApprovalWorkflowService.ts`                                                                 | Song song tồn tại workflow task engine                              | Đồng bộ và gắn liền với canonical entity workflow                                        | Section 31         | Trùng lặp luồng phê duyệt                                       | Rà soát và hợp nhất về canonical application service                                                                                              | `tests/domain/workflowRegression.test.ts`           | ✅ **RESOLVED** |
| **WF-018**   | `BatchAppService.ts:182`                                                                     | Dùng `currentBatch` từ caller/cache để quyết định status mutation   | Luôn đọc fresh Batch trực tiếp từ database trước khi chuyển trạng thái                   | Section 32 & 33    | Caller truyền `options.currentBatch` cũ                         | Buộc đọc lại: `const currentBatch = await this.repo.findById(batchId)`                                                                            | `tests/domain/batchWorkflowRegression.test.ts`      | ✅ **RESOLVED** |
| **WF-019**   | `BatchAppService.ts:246`                                                                     | Client truyền `batchTestResults` cũ để duyệt Release                | Lấy fresh test results từ repository canonical                                           | Section 34         | Cache client có thể cũ                                          | Fresh read test results từ repository                                                                                                             | `tests/domain/workflowRegression.test.ts`           | ✅ **RESOLVED** |
| **WF-020**   | Architecture Testing                                                                         | Chưa có static guard phát hiện vi phạm status mutation              | Static guard bắt toàn bộ caller trái phép                                                | Section 35         | Thiếu architecture automated testing                            | Tạo `src/architecture/workflowMutationGuard.test.ts`                                                                                              | `src/architecture/workflowMutationGuard.test.ts`    | ✅ **RESOLVED** |
| **WF-021**   | Contract Definition                                                                          | Nhập nhằng giữa TestResult `RELEASED` và Batch `RELEASED`           | TestResult chỉ đến `APPROVED`; Batch mới có trạng thái `RELEASED`                        | Section 25         | Nhầm lẫn giữa thực thể TestResult và Batch                      | Chuẩn hóa contract: TestResult: `DRAFT -> SUBMITTED -> FINAL -> APPROVED -> SUPERSEDED`; Batch: `PENDING -> TESTING -> RELEASED/REJECTED/BLOCKED` | `tests/domain/testResultWorkflowRegression.test.ts` | ✅ **RESOLVED** |
| **WF-022**   | Repo-wide Audit                                                                              | Các luồng CRUD/AI/auto-create tiềm ẩn status mutation               | Quét và loại bỏ 100% direct status mutation ngoài Service                                | Section 36 & 37    | Vi phạm phân tán rải rác                                        | Quét toàn bộ codebase, loại bỏ mọi điểm đột biến trạng thái                                                                                       | `src/architecture/workflowMutationGuard.test.ts`    | ✅ **RESOLVED** |
