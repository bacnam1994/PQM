# 🛡️ PQM WORKFLOW ENFORCEMENT MATRIX

## MA TRẬN CƯỠNG CHẾ VẬN HÀNH QUY TRÌNH HỆ THỐNG Ở RUNTIME (RUNTIME ENFORCEMENT MATRIX)

> **Căn cứ tối cao:** `PQM_SYSTEM_WORKFLOW_MASTER.md` (Model 00)  
> **Nguyên tắc định danh:** Một workflow chỉ được đánh dấu **`ENFORCED`** khi đã thỏa mãn 100% các thành phần:  
> `UI Entry` $\to$ `Hook` $\to$ `App Service` $\to$ `Domain Rule` $\to$ `State Machine` $\to$ `Repository` $\to$ `Security` $\to$ `Audit` $\to$ `Test Evidence`.  
> Tuyệt đối không đánh dấu `ENFORCED` nếu chỉ có tài liệu đặc tả mà thiếu rào chắn runtime hoặc thiếu test chứng minh.

---

## 1. BẢNG MA TRẬN CƯỠNG CHẾ QUY TRÌNH (15 CORE WORKFLOWS)

| #   | Workflow                        | UI Entry                                                 | Hook                                              | App Service                                             | Domain Rule                                                                                                                                     | State Machine                                                                    | Repository                                    | Security Enforcement                                                                       | Audit Trail                                                        | Test Evidence                                                                                             |  Trạng thái  |
| --- | :------------------------------ | :------------------------------------------------------- | :------------------------------------------------ | :------------------------------------------------------ | :---------------------------------------------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------------------- | :-------------------------------------------- | :----------------------------------------------------------------------------------------- | :----------------------------------------------------------------- | :-------------------------------------------------------------------------------------------------------- | :----------: |
| 1   | **Product creation**            | `ProductFormPage.tsx`                                    | `useProductForm.ts`, `useProductQueries.ts`       | `ProductAppService.ts`                                  | `can(user, 'product:create')`, `productSchema` (Zod), duplicate code guard                                                                      | N/A (Lifecycle: ACTIVE / DISCONTINUED)                                           | `FirebaseProductRepository.ts`                | `database.rules.json` (ADMIN/QA write only), Fail-Closed query                             | `logAuditAction` (`CREATE`, `PRODUCTS`)                            | `ProductAppService.test.ts`, `queries.test.tsx`                                                           | **ENFORCED** |
| 2   | **TCCS creation/update**        | `TCCSFormPage.tsx`                                       | `useTCCSForm.ts`, `useTCCSQueries.ts`             | `TCCSAppService.ts`                                     | `TCCSRules.validateCriteriaDefinitions`, `min <= max` check, cross-product guard                                                                | `ApprovalWorkflowService` (TCCS Review)                                          | `FirebaseTCCSRepository.ts`                   | `database.rules.json` (QA/ADMIN write only), Fail-Closed query                             | `logAuditAction` (`CREATE`/`UPDATE`, `TCCS`)                       | `TCCSAppService.test.ts`, `model6Regression.test.ts`                                                      | **ENFORCED** |
| 3   | **Formula management**          | `ProductFormulaFormPage.tsx`                             | `useFormulaForm.ts`, `useFormulaQueries.ts`       | `FormulaAppService.ts`                                  | `formulaSchema` (Zod), duplicate ingredient guard, bound to Product                                                                             | N/A (Draft $\to$ Approved $\to$ Obsolete)                                        | `FirebaseFormulaRepository.ts`                | `database.rules.json` (QA/ADMIN write only), Fail-Closed query                             | `logAuditAction` (`CREATE`/`UPDATE`, `FORMULAS`)                   | `FormulaAppService.test.ts`, `basisCalculation.test.ts`                                                   | **ENFORCED** |
| 4   | **Batch creation**              | `BatchFormPage.tsx`                                      | `useBatchForm.ts`, `useBatchQueries.ts`           | `BatchAppService.ts`                                    | `BatchRules.canCreate`, schema snapshotting (TCCS & Formula), biological expDate guard (`expDate > mfgDate`)                                    | `BatchStateMachine` (Initial state: `PENDING`)                                   | `FirebaseBatchRepository.ts`                  | `database.rules.json` (PRODUCTION/QA/ADMIN), Fail-Closed query                             | `logAuditAction` (`CREATE`, `BATCHES`)                             | `BatchAppService.test.ts`, `model6Regression.test.ts`                                                     | **ENFORCED** |
| 5   | **Test Result entry**           | `TestResultFormPage.tsx`                                 | `useTestResultForm.ts`, `useTestResultQueries.ts` | `TestResultAppService.ts`                               | `testResultSchema` (Zod), `EntityIdentityManager` (batch.id check), `isPass: boolean \| null`                                                   | `TestResultWorkflowStateMachine` (`DRAFT`)                                       | `FirebaseTestResultRepository.ts`             | `database.rules.json` (LAB/QC/QA/ADMIN), Fail-Closed query                                 | `logAuditAction` (`CREATE`/`UPDATE`, `TEST_RESULTS`)               | `TestResultAppService.test.ts`, `model1Regression.test.ts`                                                | **ENFORCED** |
| 6   | **Quality Evaluation**          | `CriteriaInputGroup.tsx`, `BatchDetailPage.tsx`          | `useCriteriaResolver.ts`                          | `QualityEvaluationEngine.ts`                            | `CriterionEvaluator.ts`, `AlternateRuleEvaluator.ts`, `OverallResultEvaluator.ts`, Epsilon $10^{-9}$ floating protection                        | `CanonicalStatusResolver.ts` (Deterministic Engine: PASS/FAIL/PENDING/UNKNOWN)   | Read-only computation on raw results          | UI cannot override domain conclusion (AI/UI cannot decide PASS)                            | ALCOA+ Snapshot sealing                                            | `QualityEvaluationEngine.parity.test.ts` (72 parity tests), `evaluationEdgeCases.test.ts` (18 edge cases) | **ENFORCED** |
| 7   | **Finalization (Chốt kết quả)** | `TestResultFormPage.tsx`                                 | `useTestResultSave.ts`                            | `TestResultAppService.ts`                               | `QualityWorkflowMatrixGuard`: Cấm chốt `FINAL` khi chất lượng còn `PENDING` hoặc `UNKNOWN`                                                      | `TestResultWorkflowStateMachine` (`SUBMITTED` $\to$ `FINAL`)                     | `FirebaseTestResultRepository.ts`             | QC/QA/ADMIN role required; locked from general editing                                     | `logAuditAction` (`UPDATE`, `TEST_RESULTS`)                        | `workflowEnforcement.test.ts`, `TestResultAppService.test.ts`                                             | **ENFORCED** |
| 8   | **QA Approval**                 | `ApprovalWorkflowModal.tsx`, `TestResultDetailPage.tsx`  | `useApprovalWorkflow.ts`                          | `ApprovalWorkflowService.ts`, `TestResultAppService.ts` | `TestResultRules.canApprove`, cấm duyệt `PASS` khi có chỉ tiêu vi phạm                                                                          | `TestResultWorkflowStateMachine` (`FINAL` $\to$ `APPROVED`)                      | `FirebaseTestResultRepository.ts`             | QA/ADMIN role required; FDA 21 CFR Part 11 Electronic Signature required                   | `AlcoaAuditManager`, cryptographic snapshot hash seal              | `ApprovalWorkflowService.test.ts`, `signatureService.test.ts`                                             | **ENFORCED** |
| 9   | **Release Gate**                | `BatchDetailPage.tsx`, `BatchList.tsx`                   | `useBatchList.ts`, `useBatchQueries.ts`           | `BatchAppService.ts`                                    | `ReleaseRules.evaluateReleasePrerequisites` (7 mandatory gates), biological expiry check, zero open critical deviations                         | `BatchStateMachine` (`TESTING` $\to$ `RELEASED`; cấm `PENDING` $\to$ `RELEASED`) | `FirebaseBatchRepository.ts`                  | QA/ADMIN role required; Part 11 E-Signature required; database rule locks record           | `logAuditAction`, immutable cryptographic audit log                | `BatchAppService.test.ts`, `model13Regression.test.ts`, `workflowEnforcement.test.ts`                     | **ENFORCED** |
| 10  | **CoA Generation**              | `CoAReportPage.tsx`, `CoAVerifyPage.tsx`                 | `useCoAReport.ts`                                 | `reportService.ts`                                      | Sealed `evaluationSnapshot` required; SHA-256 hash integrity check; no division-by-zero                                                         | N/A (Read-only verified artifact)                                                | Targeted indexed query                        | Token/QR cryptographic verification, public read-only route                                | Logged verification access                                         | `reportService.test.ts`, `historicalIntegrity.test.ts`                                                    | **ENFORCED** |
| 11  | **Genealogy & Lineage**         | `Batch360Page.tsx`, `Product360Page.tsx`                 | `useDataGraph.ts`                                 | `DataLineageManager.ts`, `batchGenealogyService.ts`     | Forward & backward traceability: Batch $\to$ TCCS $\to$ Formula $\to$ Raw Materials $\to$ Test Results $\to$ Deviations                         | Preserves `PENDING`/`UNKNOWN` states without false positive alarms               | $O(1)$ Hash Index Cache                       | Read-only aggregation                                                                      | Lineage explanation audit trail                                    | `model4Regression.test.ts`, `batchGenealogy.test.ts`                                                      | **ENFORCED** |
| 12  | **Reconciliation**              | `DataConsistencyCenter.tsx`                              | `useDataConsistency.ts`                           | `ConsistencyAuditor.ts`, `dataConsistencyService.ts`    | 8-category consistency classification (`MISSING`, `INCOMPLETE`, `CONTRADICTORY`, `STALE`, `ORPHAN`, `DUPLICATE`, `INVALID`, `DERIVED_MISMATCH`) | `CollectionLoadState` management                                                 | `EntityIdentityManager.auditEntityIdentities` | Detection only; does not mutate data during scan                                           | `ConsistencyReport` generation                                     | `model7Regression.test.ts`, `dataConsistencyService.test.ts`                                              | **ENFORCED** |
| 13  | **Auto-Heal**                   | `DataConsistencyCenter.tsx`                              | `useAutoHealing.ts`                               | `AutoHealingFramework.ts`                               | Inviolable GMP Guard: `NEVER_AUTO_HEAL` on approved/released records, raw lab measurements, or signatures                                       | Atomic transaction execution (All-or-Nothing rollback on failure)                | Atomic multi-path updates                     | QA/ADMIN required for `CONTROLLED_HEAL`, auto-rejection of forbidden targets               | Post-heal invariant verification & audit logging                   | `model8Regression.test.ts`, `model13Regression.test.ts`                                                   | **ENFORCED** |
| 14  | **ALCOA+ Audit Trail**          | `AuditLogPage.tsx`                                       | `useAuditLogs.ts`                                 | `AlcoaAuditManager.ts`, `auditService.ts`               | 21 CFR Part 11.10(e) compliant: Genesis hash, continuous cryptographic SHA-256 hash chaining, frozen memory (`Object.freeze`)                   | Immutable append-only chain                                                      | Append-only store (`audit_logs/`)             | `database.rules.json` cấm `.write` update/delete ngay cả với ADMIN                         | Tamper verification (`verifyChainIntegrity`), inspection reporting | `model9Regression.test.ts`, `model13Regression.test.ts`                                                   | **ENFORCED** |
| 15  | **AI-assisted workflow**        | `TestResultFormPage.tsx` (AI OCR), `AIAssistantChat.tsx` | `useTestResultAIIntegration.ts`                   | `AIGateway.ts`, `aiActionGuard.ts`                      | AI is strictly ADVISORY: OCR extraction, semantic dictionary mapping, draft suggestion. Zero direct database writes.                            | Proposal state machine: `PROPOSED` $\to$ `VALIDATED` $\to$ `HUMAN_APPROVED`      | Blocked from direct repository calls          | `aiActionGuard.validateAIAction` blocks unauthorized execution, privilege escalation check | Audit log of all AI proposals & human signoffs                     | `aiGovernance.test.ts`, `aiActionGuard.test.ts`, `TestResultFormPage.test.tsx`                            | **ENFORCED** |

---

## 2. RUNTIME ENFORCEMENT SUMMARY

- **Tổng số quy trình quản lý:** 15/15 quy trình.
- **Trạng thái thực thi:** **100% ENFORCED**.
- **Biên giới an ninh:**
  - Tầng Client / UI: Chỉ thu thập dữ liệu và gọi Application Service; không sở hữu thẩm quyền kinh doanh.
  - Tầng Application / Domain: Cưỡng chế State Machine (`BatchStateMachine`, `TestResultWorkflowStateMachine`), Domain Rules (`BatchRules`, `ReleaseRules`), và Single Source of Truth (`QualityEvaluationEngine`).
  - Tầng Repository: Áp dụng chính sách `QueryPolicy` với cơ chế **Fail-Closed** cho 100% Regulated Collections (`batches`, `testResults`, `products`, `tccsList`, `productFormulas`, `audit_logs`).
  - Tầng Database: `database.rules.json` đóng vai trò chốt chặn cuối cùng ngăn chặn can thiệp trực tiếp từ client.
